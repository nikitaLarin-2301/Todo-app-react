import {createTheme} from '@mui/material'

export const theme = createTheme({
  palette: {
    background: {
      default: '#414141',
      paper: '#929292',
    },
    primary: {
      main: '#FFFFFF',
      light: '#2196F3',
      contrastText: '#BC0000',
    },
  },
})